// Fill this in after deploying the Apps Script web app.
// Leave apiUrl empty to use browser-local storage until a real API is connected.
window.SDS_CONFIG = {
  apiUrl: "",
  adminUrl: "",
  publicReadOnly: true
};
